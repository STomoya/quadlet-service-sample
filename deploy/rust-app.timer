[Unit]
Description=Run my Rust App every 5 minutes

[Timer]
# Every 5 minutes on the clock
OnCalendar=*:0/5
Persistent=true
# Prevent "thundering herd" CPU spikes
RandomizedDelaySec=10s
AccuracySec=1s

[Install]
WantedBy=timers.target
